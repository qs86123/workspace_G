package com.changhong.data.jxw.ktr.mapper;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface ApprovalDPMapper
{
    // @Transactional
    // @Update("update data_package set description = #{dataPackage.description} where id=#{dataPackage.id}")
    // public void updateApprovalDP(@Param("dataPackage") DataPackage dataPackage);
    //
    // @Select("select * from data_package where id=#{id}")
    // DataPackage findDataPackageById(@Param("id") String id);

}
